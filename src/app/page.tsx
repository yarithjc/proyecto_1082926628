/**
 * Home Page — Client Component
 *
 * Página de login del sistema.
 * Componente client-side que maneja autenticación.
 */

import Login from "@/components/Login/Login";

export default function HomePage() {
  return <Login />;
}

